#!/bin/bash

if (( $# < 2 )); then
  echo "usage: host port"
  exit 1
fi

HOST=$1
PORT=$2

echo -e "Connecting to $HOST:$PORT via ssh..."

read -p "Username: " NAME
exec ssh -o PubkeyAuthentication=no -p $PORT $NAME@$HOST
