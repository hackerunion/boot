- copying as solution to volume issue may not be terrible - we can leave the one volume going in RO mode while we prep the next one?
- need to ensure private files are private (o-rwx...) (i.e., tokens / perms / etc)
- need to not share secret in env
- live terminal
- passwd.json not readable?
